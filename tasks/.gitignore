# Файл-мост Programmer↔оркестратор и служебное состояние сканера — транзиентные,
# в репозиторий не коммитим (содержимое всё время меняется фидером/сканером).
claude-tasks.json
.scanner-state.json
.intake-state.json
*.tmp
