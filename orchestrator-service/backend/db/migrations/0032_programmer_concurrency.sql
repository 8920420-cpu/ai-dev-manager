-- =====================================================================
-- APP-SETTINGS-002 — параллелизм исполнителя PROGRAMMER (стадия CODING).
-- =====================================================================
-- Сколько задач программиста идут одновременно. Каждая исполняется в git
-- worktree СВОЕГО микросервиса (задачи одного сервиса сериализуются, разные —
-- параллельно). Жёсткий потолок — 3 (валидируется в appSettings.js).
-- =====================================================================
BEGIN;

INSERT INTO app_settings (key, value)
VALUES ('programmer_concurrency', '3'::jsonb)
ON CONFLICT (key) DO NOTHING;

COMMIT;
