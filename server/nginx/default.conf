server {
    listen 80;
    server_name _;
    root /srv/www;

    location = /health {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }

    location / {
        autoindex on;
        try_files $uri $uri/ =404;
    }
}
